{{template "base" .}}
{{define "content"}}

<div class="container">
    <div class="row">
        <div class="column">
            <h>This header eli H tagi</h>
            <p>This came from template data: {{index .StringMap "test"}}</p>
            <p>
             {{if ne  (index .StringMap "remote_ip" ) ""}}
                Your IP address is {{index .StringMap "remote_ip"}}
            {{else}}
                I dont know your IP address, visit home page <a href="/">Home </a>
            {{end}}
            </p>

        </div>
    </div>
</div>

{{end}}